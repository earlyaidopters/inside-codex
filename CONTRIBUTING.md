# Working on Inside Codex

Start with README.md, the latest release entry at the end of production/STATUS.md, and the production document for the mechanism you are changing. Older status entries are historical snapshots, including their old blockers and next steps.

1. Keep learning rules and validators in the lab modules. Keep rendering and picking in exhibits. Preserve exercise/progress boundaries when adding exploration.
2. Make art changes in a candidate path. Preserve active masters, runtime assets, and their revision manifests until the candidate is verified.
3. Run `npm test` and `npm run build` in game. Choose browser checks appropriate to the changed mechanism; use fresh evidence paths and inspect the actual rendered result.
4. Do not turn simulated outcomes, assistant aesthetic ratings, or one-Mac timing observations into universal product or learning claims. Date capability sources.
5. Scan staged changes with `gitleaks git --pre-commit --staged --redact`. The narrow documentation exception in .gitleaks.toml covers one non-secret example UUID only.
6. Review the staged file list before committing. Do not add credentials, private hosting state, installed dependencies, duplicate builds, or raw checkpoint archives. `studio.env` is the runtime environment-lighting asset and belongs in Git.
7. Treat deployment separately from source control. Build and freeze the release, run relevant checks, compare remote version state before publishing, then verify the hosted file hashes and browser journey.

The companion slide source under slides is a snapshot of a separate local authoring project. Its copied dist directory contains the editable HTML/CSS/JavaScript. No repository action deploys those slides automatically.
