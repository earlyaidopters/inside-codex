import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {estimateTextureStorage} from '../src/texture-storage.mjs';
test('BC7 and ASTC count whole 16-byte blocks through tiny mip levels',()=>{for(const format of [0x8e8c,0x8e8d,0x93b0,0x93d0]){assert.equal(estimateTextureStorage({width:1,height:1,format,type:0}).textureBytes,16);assert.equal(estimateTextureStorage({width:7,height:5,format,type:0,generateMipMaps:true}).textureBytes,96);assert.equal(estimateTextureStorage({width:2048,height:2048,format,type:0,generateMipMaps:true}).textureBytes,5592432);}});
test('Uncompressed and multisample storage retain their original arithmetic',()=>{assert.deepEqual(estimateTextureStorage({width:2048,height:2048,format:5,type:0,generateMipMaps:true,samples:4}),{bytesPerPixel:4,blockBytes:null,textureBytes:22369620,estimatedMultisampleColorBytes:67108864});assert.equal(estimateTextureStorage({width:4,height:4,format:999999,type:0}).textureBytes,null);});
test('Every texture in the sealed uncompressed baseline retains the same estimate',()=>{const root=new URL('../assets/generated/.game-dev/runs/run_1789048295204_f776da21276e498ea69e456c6b6d7af8/traversal.json',import.meta.url);const run=JSON.parse(fs.readFileSync(root));for(const point of [run.initial,run.final])for(const t of point.world.renderAudit.textures){const estimate=estimateTextureStorage({...t,generateMipMaps:t.mipmaps,isCube:t.cube});assert.equal(estimate.textureBytes,t.textureBytes,t.names.join(','));assert.equal(estimate.estimatedMultisampleColorBytes,t.estimatedMultisampleColorBytes);}});
